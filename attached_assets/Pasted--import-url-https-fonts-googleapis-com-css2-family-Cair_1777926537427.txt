@import url('https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700;800;900&family=Tajawal:wght@300;400;500;700;800&display=swap');

@tailwind base;
@tailwind components;
@tailwind utilities;

/* ================================================
   LIGHT MODE — Tuwaiq Warm Green Identity
   Inspired by taseel.org.sa aesthetic
================================================ */
:root {
  /* Primary: Tuwaiq Deep Green */
  --primary: 152 42% 28%;
  --primary-foreground: 0 0% 100%;

  /* Secondary: Deep Navy */
  --secondary: 211 40% 28%;
  --secondary-foreground: 0 0% 100%;

  /* Accent: Warm Bronze/Gold (CTA like taseel) */
  --accent: 28 44% 59%;
  --accent-foreground: 0 0% 100%;

  /* Backgrounds — warm cream inspired by taseel */
  --background: 35 28% 97%;
  --foreground: 210 22% 14%;

  /* Card */
  --card: 0 0% 100%;
  --card-foreground: 210 22% 14%;

  /* Popover */
  --popover: 0 0% 100%;
  --popover-foreground: 210 22% 14%;

  /* Muted */
  --muted: 35 18% 93%;
  --muted-foreground: 215 15% 48%;

  /* Destructive */
  --destructive: 0 80% 52%;
  --destructive-foreground: 0 0% 98%;

  /* Borders & Inputs */
  --border: 35 15% 86%;
  --input: 35 15% 86%;
  --ring: 152 42% 28%;

  --radius: 0.625rem;

  --font-body: 'Tajawal', sans-serif;
  --font-heading: 'Cairo', sans-serif;

  /* Sidebar */
  --sidebar: 35 20% 96%;
  --sidebar-foreground: 210 22% 14%;
  --sidebar-primary: 152 42% 28%;
  --sidebar-primary-foreground: 0 0% 100%;
  --sidebar-accent: 152 40% 94%;
  --sidebar-accent-foreground: 152 42% 22%;
  --sidebar-border: 35 15% 86%;
  --sidebar-ring: 152 42% 28%;
  --sidebar-width: 16rem;
  --sidebar-width-mobile: 18rem;

  /* Gold tones */
  --gold: 28 44% 59%;
  --gold-light: 35 60% 85%;
  --gold-dark: 26 52% 42%;

  /* Nav warm beige (taseel #f2eae3 style) */
  --nav-bg: 35 30% 92%;
  --nav-text: 152 42% 22%;
}

/* ================================================
   BASE LAYER
================================================ */
@layer base {
  * {
    @apply border-border;
  }

  html {
    direction: rtl;
    scroll-behavior: smooth;
  }

  body {
    @apply bg-background text-foreground antialiased;
    font-family: var(--font-body), sans-serif;
    min-height: 100vh;
    min-height: -webkit-fill-available;
    overflow-x: clip;
    width: 100%;
    max-width: 100%;
    position: relative;
    background-color: hsl(35 28% 97%);
  }

  #root {
    width: 100%;
    max-width: 100%;
    overflow-x: clip;
    position: relative;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: var(--font-heading), sans-serif;
    @apply font-bold tracking-tight;
  }

  ::selection {
    background: hsl(var(--primary) / 0.15);
    color: hsl(var(--foreground));
  }

  /* iOS Safari text size */
  -webkit-text-size-adjust: 100%;
  text-size-adjust: 100%;
}

/* ================================================
   COMPONENT LAYER
================================================ */
@layer components {

  /* --- Gradient Text --- */
  .text-gradient {
    @apply bg-clip-text text-transparent;
    background-image: linear-gradient(135deg, hsl(var(--primary)), hsl(152 55% 42%));
  }

  .text-gradient-gold {
    @apply bg-clip-text text-transparent;
    background-image: linear-gradient(135deg, hsl(var(--gold-dark)), hsl(var(--gold)));
  }

  /* --- Brand Gradient Background --- */
  .bg-gradient-brand {
    background-image: linear-gradient(135deg, hsl(var(--primary)), hsl(152 48% 36%));
  }

  .bg-gradient-gold {
    background-image: linear-gradient(135deg, hsl(var(--gold-dark)), hsl(var(--gold)));
  }

  /* --- Nav bar style (taseel-inspired warm beige) --- */
  .nav-topbar {
    background-color: hsl(var(--primary));
    color: white;
  }

  .nav-mainbar {
    background-color: hsl(var(--nav-bg));
    border-bottom: 1px solid hsl(35 20% 84%);
  }

  /* --- CTA Donate Button (Gold/Bronze - taseel style) --- */
  .btn-donate {
    @apply relative overflow-hidden font-heading font-bold transition-all duration-300 rounded-lg;
    background-color: hsl(var(--gold));
    color: white;
    box-shadow: 0 2px 12px hsl(var(--gold) / 0.35);
  }

  .btn-donate:hover {
    background-color: hsl(var(--gold-dark));
    box-shadow: 0 4px 20px hsl(var(--gold) / 0.5);
    transform: translateY(-1px);
  }

  /* --- Primary Button --- */
  .btn-primary {
    @apply relative overflow-hidden font-heading font-bold transition-all duration-300 rounded-lg;
    background-color: hsl(var(--primary));
    color: white;
    box-shadow: 0 2px 12px hsl(var(--primary) / 0.3);
  }

  .btn-primary:hover {
    background-color: hsl(152 42% 24%);
    box-shadow: 0 4px 20px hsl(var(--primary) / 0.45);
    transform: translateY(-1px);
  }

  /* --- Glass Morphism Cards --- */
  .glass-card {
    background: hsl(var(--card) / 0.8);
    backdrop-filter: blur(16px) saturate(1.4);
    -webkit-backdrop-filter: blur(16px) saturate(1.4);
    border: 1px solid hsl(var(--border));
    box-shadow: 0 2px 12px hsl(var(--foreground) / 0.05);
  }

  /* --- Clean Card (taseel-style) --- */
  .clean-card {
    @apply bg-white rounded-xl transition-all duration-300;
    border: 1px solid hsl(35 15% 88%);
    box-shadow: 0 1px 4px hsl(var(--foreground) / 0.05);
  }

  .clean-card:hover {
    border-color: hsl(var(--primary) / 0.25);
    box-shadow: 0 6px 28px hsl(var(--primary) / 0.1);
    transform: translateY(-2px);
  }

  /* --- Luxury Card --- */
  .luxury-card {
    @apply bg-white rounded-xl transition-all duration-500;
    border: 1px solid hsl(var(--border));
    box-shadow: 0 1px 3px hsl(var(--foreground) / 0.05);
  }

  .luxury-card:hover {
    border-color: hsl(var(--primary) / 0.3);
    box-shadow: 0 8px 36px hsl(var(--primary) / 0.12);
    transform: translateY(-3px);
  }

  /* --- Section Header decoration --- */
  .section-label {
    @apply text-xs font-bold uppercase tracking-widest;
    color: hsl(var(--gold));
  }

  /* --- Hover Elevation --- */
  .hover-elevate {
    @apply transition-all duration-300;
  }

  .hover-elevate:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 32px hsl(var(--primary) / 0.12);
  }

  /* --- Navbar Glass --- */
  .navbar-glass {
    background: hsl(var(--nav-bg) / 0.92);
    backdrop-filter: blur(20px) saturate(1.5);
    -webkit-backdrop-filter: blur(20px) saturate(1.5);
    border-bottom: 1px solid hsl(35 20% 84%);
  }

  /* --- Premium Button --- */
  .btn-premium {
    @apply relative overflow-hidden rounded-lg font-heading font-bold transition-all duration-300;
    background: linear-gradient(135deg, hsl(var(--primary)), hsl(152 48% 36%));
    color: white;
    box-shadow: 0 4px 20px hsl(var(--primary) / 0.3);
  }

  .btn-premium:hover {
    box-shadow: 0 6px 28px hsl(var(--primary) / 0.45);
    transform: translateY(-1px);
  }

  /* --- Badge --- */
  .badge-premium {
    @apply inline-flex items-center gap-1.5 text-xs font-bold px-3 py-1 rounded-full;
    background: hsl(var(--primary) / 0.1);
    color: hsl(var(--primary));
    border: 1px solid hsl(var(--primary) / 0.2);
  }

  .badge-gold {
    @apply inline-flex items-center gap-1.5 text-xs font-bold px-3 py-1 rounded-full;
    background: hsl(var(--gold) / 0.12);
    color: hsl(var(--gold-dark));
    border: 1px solid hsl(var(--gold) / 0.25);
  }

  /* --- Stat Card --- */
  .stat-card {
    @apply luxury-card p-6 text-center;
  }

  /* --- Islamic Pattern Overlay --- */
  .islamic-pattern {
    background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.04'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
  }

  /* --- Shimmer --- */
  .gold-shimmer {
    background: linear-gradient(
      90deg,
      hsl(var(--gold-dark)) 0%,
      hsl(var(--gold)) 40%,
      hsl(var(--gold-light)) 50%,
      hsl(var(--gold)) 60%,
      hsl(var(--gold-dark)) 100%
    );
    background-size: 200% 100%;
    animation: shimmer 3s ease infinite;
  }

  @keyframes shimmer {
    0% { background-position: 200% center; }
    100% { background-position: -200% center; }
  }

  /* --- Section Divider --- */
  .section-divider {
    height: 1px;
    background: linear-gradient(90deg, transparent, hsl(var(--border)), transparent);
  }

  /* --- Announcement ticker --- */
  .ticker-wrap {
    overflow: hidden;
    white-space: nowrap;
  }

  .ticker-move {
    display: inline-block;
    animation: ticker 30s linear infinite;
  }

  @keyframes ticker {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(100%); }
  }

  /* ─── WhatsApp Floating Button ─── */
  .whatsapp-float {
    position: fixed;
    bottom: 1.5rem;
    left: 1.5rem;
    z-index: 9999;
    padding-bottom: env(safe-area-inset-bottom, 0);
  }

  /* Sidebar width */
  .sidebar-width {
    width: var(--sidebar-width);
  }

  /* tap highlight for iOS */
  * {
    -webkit-tap-highlight-color: transparent;
  }
}

/* ================================================
   DARK MODE
================================================ */
.dark {
  --primary: 152 42% 42%;
  --primary-foreground: 0 0% 100%;
  --secondary: 211 40% 50%;
  --secondary-foreground: 0 0% 100%;
  --accent: 28 44% 62%;
  --accent-foreground: 0 0% 10%;
  --background: 220 25% 10%;
  --foreground: 35 20% 92%;
  --card: 220 22% 14%;
  --card-foreground: 35 20% 92%;
  --popover: 220 22% 14%;
  --popover-foreground: 35 20% 92%;
  --muted: 220 18% 20%;
  --muted-foreground: 215 15% 58%;
  --border: 220 18% 22%;
  --input: 220 18% 22%;
  --ring: 152 42% 42%;
  --sidebar: 220 22% 12%;
  --sidebar-foreground: 35 20% 88%;
  --sidebar-border: 220 18% 22%;
  --nav-bg: 220 22% 14%;
}
